CREATE TABLE player_info(
    id   int PRIMARY KEY NOT NULL,
    name char(255),
    card_num char(20),
    item char(255),
    clothes_size char(255),
    play_number char(255),
    phone_number char(30)
);

COMMIT;
